<script class="code" type="text/javascript">
	$(document).ready(function(){
	 $.getJSON('/documents.json?source=<%=source.id%>', function(data){
	  var plot1 = $.jqplot('source_<%=source.id%>', [data], sl_opts );
	});
});
</script>  

<tr>
	<td class="toggle">
		<%=link_to short_source(source), source%>
		<%= abutton_to 'Edit', edit_source_path(source),{style:'btn-small',icon:'edit'}%>

		
	</td>
	<td><%=link_to source.documents.count, documents_path(:source=>source) %>
		</td>
	 <td> <div class="sparkline" id="source_<%=source.id%>"></div></td>
	<td>
	<%= abutton_to 'Update from feed', import_source_url(source),{style:'btn-small pull-right',icon:'refresh'} %> 
	 </td>
	<td>
		<div class="btn-group pull-right">
	<%= abutton_to 'Reset', reset_source_path(source), 
	{method:'get',:style=>'btn btn-small',icon:'fire'}%>
	<%= abutton_to 'Delete', source_url(source), 
	{method:'delete',:style=>'btn btn-small btn-danger',icon:'trash icon-white'} %>
	</div>
	</td>
</tr>