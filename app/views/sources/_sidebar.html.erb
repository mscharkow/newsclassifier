<table class="table table-striped table-condensed">
	<thead><tr><th>Source</th><th>Documents</th></thead>
		<tbody>
<% @project.sources.order(:name).each do |source| %>

<tr>
	<td><%= link_to short_source(source,20), source_path(source)%></td>
	<td><%= source.documents.size %></td>
</tr>
<% end %>
</tbody>
</table>
<p>
<%= abutton_to 'Add new source', new_source_path, {:style=>'btn-small'}%>
</p>
